{% extends 'admin/layoutAdmin.njk' %}

{% block content %}

    <div class="management">
        <h1 class="admin">Gerenciar chefs</h1>
        <button><a  href="/admin/chefs/create">Novo chef</a></button>
    </div>

    <section class="chefs">

        {% for chef in chefs %}
            <div class="chef" id="{{ chef.id }}">
                <img src="{{ chef.avatar_url }}" alt="{{ chef.name }}">
                <div>
                    <h3>{{ chef.name }}</h3>
                    <a href="/admin/chefs/{{ chef.id }}">Visualizar</a>
                </div>
            </div>
        {% endfor %}

        <div class="chefSectionSuport" id="">
            <img src="">
        </div>

        <div class="chefSectionSuport" id="">
            <img src="">
        </div>
        
        </section>

{% endblock content %} 